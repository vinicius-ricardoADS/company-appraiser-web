import classes from './Footer.module.css';

const Footer = () => {
    return (
        <footer className={classes.footer}>
            <p className={classes.authorization}>© Todos os direitos reservados</p>
        </footer>
    )
};

export default Footer;